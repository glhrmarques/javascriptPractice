// It's a common practice to declare arrays with const 

//Acessing the element by referring its index number
const cars = ['Slow', 'Fast']
let car = cars[1]

//Objects use named indexes to access its 'members', on the other hand, arrays use numbers.
const nameGenerator = [firstName= 'Guilherme', secondName='Andressa'];
const carGenerator = ['Carro1', 'Carro2'];

let name = firstName;
let carPrint = carGenerator[1]

//Array Properties
const fruits = ['Passion fruit', 'Strawberry', 'Grape']
let length = fruits.length;

//looping the array


//push() adds a new element to the array.]
const testing = ['Passion fruit', 'Strawberry', 'Grape']
testing.push('Lemon');
